//Challenge: Validate a palindrome
//  A sequence that read the same backwards as forwards
//Expected Run : ('racecar) => true , ('coin') => false

function palindrome(s){

    //Reverse the String 
        //Push to array , reverse and parse to string
    //Return true if revString and str is equals

    const revString = str.split('').reverse().join('');

    return revString === str;

}
